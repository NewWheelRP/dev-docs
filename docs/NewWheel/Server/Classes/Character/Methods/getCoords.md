Returns the character coords

```ts
// returns coords: Vector4
character.getCoords();
```

The vector4 type:

```ts
type Vector4 = {
	x: number; // X coord
	y: number; // Y coord
	z: number; // Z coord
	w: number; // Heading
};
```
